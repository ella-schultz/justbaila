{
  "name": "justbaila-inner-circle",
  "private": true,
  "scripts": {
    "cards:generate": "node scripts/generate-cards.mjs",
    "worker:dev": "wrangler dev --config worker/wrangler.toml",
    "worker:deploy": "wrangler deploy --config worker/wrangler.toml",
    "db:migrate:local": "wrangler d1 migrations apply justbaila-inner-circle --local --config worker/wrangler.toml",
    "db:migrate:remote": "wrangler d1 migrations apply justbaila-inner-circle --remote --config worker/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.40.2"
  }
}
